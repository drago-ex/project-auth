## Drago Project auth
A package that extends the basic Drago Project with login, registration, and user password reset. It also creates a protected entry to the Admin section.

## Install
```bash
composer config --no-plugins allow-plugins.drago-ex/project-auth true
composer require drago-ex/project-auth
```
